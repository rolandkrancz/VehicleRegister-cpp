#pragma once

#include <string>

typedef struct VehicleViewModel {
    std::string registrationNumber;
    std::string model;
    std::string vehicleType;
    std::string firstName;
    std::string address;
} VehicleViewModel;